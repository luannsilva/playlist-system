// During bootstrap, the scaffold replaces this line with the selected theme.
// Keep only ONE export active.

export { playlistTheme as theme } from 'playlist-system/adapters/web/playlist.theme';
// export { smartdeskTheme as theme } from 'playlist-system/adapters/web/smartdesk.theme';
